{%- if image != blank -%}
    {% if image.width > 180 %}{{ image | img_url: '180x', format: 'pjpg' }} 180w {{ 180 | divided_by: image.aspect_ratio | round }}h,{% endif %}

    {% if image.width > 360 %}{{ image | img_url: '360x', format: 'pjpg' }} 360w {{ 360 | divided_by: image.aspect_ratio | round }}h,{% endif %}

    {% if image.width > 540 %}{{ image | img_url: '540x', format: 'pjpg' }} 540w {{ 540 | divided_by: image.aspect_ratio | round }}h,{% endif %}

    {% if image.width > 720  %}{{ image | img_url: '720x', format: 'pjpg' }} 720w {{ 720 | divided_by: image.aspect_ratio | round }}h,{% endif %}

    {% if image.width >= 749 %}{{ image | img_url: '749x', format: 'pjpg' }} 749w {{ 749 | divided_by: image.aspect_ratio | round }}h,{% endif %}
{% endif %}